# ===== 保研复习助手 后端依赖 =====
# Python 3.14 兼容性已验证
# 核心框架
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
aiosqlite>=0.20
python-multipart>=0.0.9   # 文件上传表单
python-dotenv>=1.0        # 读取项目根 .env（API Key 等）

# 文档解析
pymupdf>=1.24        # PDF 解析（快、目录/文字/页码）
python-docx>=1.1     # Word 解析
python-pptx>=1.0     # PPT 解析

# 全文检索（中文分词）
jieba>=0.42

# HTTP 客户端（LLM 调用，DeepSeek OpenAI 兼容接口）
httpx>=0.27
openai>=1.50

# 测试
pytest>=8.0
pytest-asyncio>=0.24

# ===== P1 可选（向量检索，默认不装，避免内存占用）=====
# fastembed>=0.4
# chromadb>=0.5
