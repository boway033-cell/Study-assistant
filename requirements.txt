# 学习助手（Study assistant）运行依赖 —— 锁定精确版本，保证不同电脑一致安装
# start.bat 首次运行会自动执行：pip install -r requirements.txt

fastapi==0.141.1
uvicorn[standard]==0.52.3
sqlalchemy==2.0.52
aiosqlite==0.22.1
python-multipart==0.0.32
python-dotenv==1.2.2

# 文档解析
pymupdf==1.28.2
python-docx==1.2.0
python-pptx==1.0.2

# 中文分词
jieba==0.42.1

# 云端 AI 客户端
httpx==0.28.1
openai==3.1.0

# API Key 加密存储
cryptography==50.0.0
