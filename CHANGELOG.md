# 变更日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [2.2.0] - 2026-09-07

- 写作工作台新增公文写作模式，覆盖材料授权、提纲确认、起草、审稿、版本保留、格式差异确认与 Word 导出；接入的第三方技能保留原许可证，非商业许可边界单独公开说明。
- 修复资料库智能视图统计被当前筛选结果覆盖的问题；全库、阅读中、收藏、待处理和未归档数量改为服务端全局统计，书架操作菜单改为无障碍图标按钮。
- OCR 与研究任务拆分为重任务、交互任务两条队列，原生 OCR 在线程中执行，不再阻塞研究报告；研究任务显示阶段、进度、取消和部分计划，断线或刷新后可通过持久化任务自动恢复。
- PDF 连续阅读器统一累计页间距，修复数百页文档深页错位与空白；画布先于文本层和按需 OCR 展示，并取消过期渲染，降低快速滚动、缩放时的卡顿和竞态。
- 大型扫描文档采用自适应 OCR 分辨率并默认关闭方向分类，补充页级心跳、缓存状态、预计剩余时间和取消检查；隔离基准中同页识别框数不变，OCR 步骤由 11.52 秒降至 9.35 秒。
- 新增 360 页合成 PDF 深页视觉回归、公文写作生产流程回归和 40 本长列表全文检索回归；测试资料均为虚构内容，不读取用户知识库。
- 本地验证：后端 173 项通过、1 项按环境条件跳过，前端 41 项通过，Vite 生产构建通过；第 176、348 页可见画布与跳页硬断言通过。

## [2.1.0] - 2026-09-05

- 修复资料库“更多”操作及筛选控件在窄内容区被裁切的问题；布局按实际容器宽度重排，展开或折叠应用侧栏时检查器与详情抽屉同步切换，菜单不受卡片边界裁切。
- 保留深青、古纸与暖赭配色。资料库主卡片新增 1.5px、25% 弧段的旋转渐变边框与 0.4 透明度辉光，常速 4 秒、悬停 2 秒；通过 Vue 与 Motion 接入机械按键弹簧反馈。
- 设置页新增界面配置：发光边框、机械按键、舒适/紧凑列表密度，支持本地保存、恢复默认和减少动画偏好；提供 128px、8px 行程的 K 键预览与按压计数。
- 修复 Word/PowerPoint 渲染版 PDF 标注错误使用结构化页数的问题，OCR 文本层改为读取实际渲染版 PDF；高亮/批注删除增加确认、错误反馈和即时界面移除。
- 阅读界面不再强制将应用导航变成无文字图标栏，保留用户主动选择的侧栏状态。
- 模型连接支持保存前识别模型列表、编辑时自动读取及手动切换；兼容常见网关列表格式和完整调用地址，保留自定义模型输入。
- 新增隔离数据浏览器回归脚本，覆盖资料库响应式菜单、键盘操作、机械按键、配置持久化和减少动画；不读取或上传用户知识库。
- 本地验证：后端 158 项通过、1 项按环境条件跳过，前端 35 项通过，24 组响应式检查及交互回归通过，浏览器无报错，Vite 生产构建通过。

## [2.0.0] - 2026-09-01

- 资料库新增服务端排序与持久化拖拽：支持自定义、导入时间、题名、作者、年份、最近阅读和阅读进度；全库与书架分别保存顺序，旧库自动迁移并保持原有视觉顺序。
- 重新编写产品首页、产品文档和下载安装指南；全部公开截图改由隔离的虚构知识库生成，截图流程不读取用户正式数据并检查禁止展示词。
- P0/P1 可靠性补全：任务中心支持失败导入显式重试，健康检查支持多选批处理、对象级失效锚点定位和安全修复；正常批注不会因同书其他知识对象失效而被重锚。
- 模型连接新增按任务配置的有序回退链与本地用量统计；只有在首个输出 token 产生前才允许回退，避免把半截回答拼接到另一模型。
- 批判性研读主张可人工修订并幂等回存笔记/证据卡；写作与 PPTX 保存来源内容快照，资料变化后明确标记陈旧或缺失来源。
- 固定检索评测集扩展至 114 个跨学科与拒答案例；基线 Recall@1 0.9706、Recall@3 0.9902、MRR 0.9853、引用正确率 0.9804、无答案拒答率 1.0000。新增 3000 本/30 万文本块 SQLite 合成容量基线与产品内容量预警。
- 目录识别新增跨学科学术章节语义：覆盖社会科学的理论/方法/案例结构、人文文学的文本细读/叙事/语境结构，以及自然科学与生物医学的材料方法、结果、透明度声明等无编号标题；目录工作台显示学术结构覆盖和待核对环节，但不自动伪造缺失标题。
- 写作工作台新增多文献综述：用户显式选择 2–50 篇库内文献后，可生成主题型叙事综述、范围梳理或证据图谱；每篇公平取样首中尾与议题相关分块，输出保留 `[B:C:P]` 稳定锚点、引用覆盖审计、Word 下载和人工编辑。
- 多文献综述提供社科、人文文学、自然生物及自动跨学科镜头；支持可选 Writing DNA 和默认去 AI 味生成约束。DNA 只约束表达，事实仍限于所选文献；生成稿可继续送入 11 条白名单逐项审阅。
- 明确方法边界：库内综述按主题比较共识、冲突、互补证据和最强反证；未执行系统检索、去重、筛选和质量评价时不得自称系统综述、元分析或 PRISMA 合规。
- 回归扩展到 `/writing` 与 `/knowledge-health`：后端 156 项、前端 33 项和 Vite 生产构建通过；13 个关键路由在桌面与 390px 移动端共 26 项 UI 冒烟全部通过。PowerPoint 视觉自动化会在不可用环境明确跳过或降级。
- AI 接入从单一 DeepSeek 升级为统一模型连接层：内置 Kimi、智谱 GLM、通义、OpenAI、Anthropic、Gemini 与自定义预设，支持 Chat Completions、Anthropic Messages、Google GenerateContent 协议，以及问答、研究、写作、PPT、通用生成的独立任务路由；旧 DeepSeek 配置无迁移继续可用。
- 模型设置页收敛为供应商列表、默认模型和两个添加入口；功能路由、视觉分析、检索与存储改为按需展开。修复新前端读取旧探测响应时出现“未连接 / 已连接”矛盾的问题，默认文本模型进入页面后独立自动检测，不再受可选视觉接口超时拖累。
- 左侧导航从功能分组改为“资料入库 → 研读与沉淀 → 写作工作台 → PPTX 汇报”四步主线；当前路径卡提供下一步提示，问答、健康、洞察及可选工具默认折叠并在访问时自动展开。
- 修复启动器复用旧后端导致“知识库健康检查 / 知识库洞察”固定路径被当作整数 ID 解析的问题；启动握手升级至 API revision 4，并强制探测 `knowledge_insights` 能力。
- 删除与个人知识库主线偏离的学习计划、考试倒排和打卡模块；升级过程不主动销毁旧 SQLite 数据表。学习统计重构为“知识库洞察”，改为衡量知识库健康、结构覆盖、原文取证、知识沉淀、来源可追溯率、证据核验与研究输出。
- 综合研读引入 scientific-critical-thinking、peer-review 与 hypothesis-generation 的方法边界：结构化标记跨文献共识、互补、冲突、证据质量、偏倚风险、竞争解释和可证伪候选假设；结果可幂等沉淀为批判性审查笔记与主张级证据卡。
- 科学写作与 PPTX 新增知识对象取材门禁：必须显式选择知识笔记、证据卡或批判性审查报告；写作语料只校准风格，原始文献片段在 PPTX 中只用于定位核查，每个事实页必须引用至少一个已选知识对象。
- 信息架构按“知识库管理 / 阅读与研究 / 创作与输出”重组；写作台从 PPTX 页隐藏抽屉提升为独立一级工作区，笔记和研究报告新增全屏沉浸阅读。
- 新增知识库健康检查，解释并定位未解析、疑似低质量 OCR、无目录、无元数据、重复资料和失效锚点；检查只读，不自动删除或静默修复用户资料。
- 锚点审计校验章节和文本块的书目归属，资料库支持从健康问题深链打开档案；修复导入完成结果错误报告 OCR 未使用的问题。

## [1.3.0] - 2026-08-29

### 改进
- 文献工作台新增“写作实验室”：用户确认语料处理权利后，可从至少 20 篇完整文章蒸馏分层 Writing DNA；语言、结构、选题/素材、认知和视觉规律分别保存，支持反馈与补充语料生成可追溯的新版本。
- Writing DNA 仿写每次加载全部 DNA 产物并选取 5 篇主题相近原文校准语感，只生成独立新作，不复制原文观点或冒充作者；结果可直接导出 Word。
- 新增白名单式“去 AI 味”：文本与 DOCX 均只处理 11 类明确痕迹，逐项记录规则编号和精确替换，保护数字、引语、链接和限定词；Word 保留原段落、表格及未改动文本格式并输出 DOCX。
- Writing DNA 全文统计改为逐篇累计，代表片段设硬上限；Word 上传和去 AI 味均采用固定缓冲及字符批次，避免大语料同时驻留内存。字体调整为分场景体系：操作控件使用微软雅黑，品牌与标题保留华文中宋，长文正文使用现代宋体；Word 输出仍以华文中宋为首选。
- 写作实验室补齐人工工作流：可核对完整语料清单、将低质量文章标记为下一版排除、并排比较历史 DNA；蒸馏中的项目会阻止并发提交下一版，避免版本竞态。
- 独立新作支持在工作台编辑标题和正文，保存时原位重建 Word；去 AI 味改为候选修改逐条勾选，文本与 DOCX 都从原稿重放所选修改，Word 段落、表格和未涉及 run 格式保持不变。
- 写作输出新增分页摘要历史和按需详情；人工修订复用原输出路径，不因每次保存产生孤立 Word 文件，文件被 Word 占用时返回明确提示。
- 目录自修正增加预览、未决冲突阻断、事务快照和历史恢复，修复层级 1→3 跳跃导致的自动修正崩溃。
- DOCX/PPTX 改用原生 Office 结构，不再被 PDF 全文标题规则二次扫描；两份异常 Office 资料已定向重解析并显著收敛目录数量。
- 研究报告、知识笔记、证据卡和 PPTX 贯通多书来源链，保留报告 ID、书目/章节范围和精确来源锚点。
- 知识记录与研究报告改为分页摘要、按需详情；任务状态改用 SSE，统计页移除逐章节 N+1 查询。
- 数据层升级到 v8；备份、日志和可再生缓存增加容量上限，普通数据与用户原文不参与自动清理。
- 修复部分 CNKI PDF 在阅读器中整页白屏或只显示零散标点/数字：PDF.js 的 CMap、标准字体与 WASM 改用站点根路径，构建时补齐按需 JBIG2 资源，开发服务器从依赖目录只读提供，不在仓库重复保存静态副本。
- “笔记与证据”新增完整笔记阅读抽屉；AI 综合报告保存为知识笔记后可按 Markdown 标题、段落和列表阅读，并保留 AI 标识、来源书目、标签、回到来源和完整内容编辑入口。
- 修复 Windows 冷启动入口：启动器改为单实例按需唤醒，以真实监听端口反查 PID，记录动态访问地址，并仅在健康检查通过后打开浏览器；失败时显示可诊断提示，不再静默留下 `127.0.0.1 拒绝连接`。
- `study-assistant://open` 安装脚本会创建桌面网页快捷方式；本地页面资源不依赖境外 CDN，服务未运行时不设守护进程或开机常驻。
- 完成“去装饰化、强层级、内容优先”统一重排：保留深青、古纸与宋体阅读气质，收敛字号、间距、圆角、阴影和语义表面，移除核心页大渐变横幅与重复英文装饰标题。
- 新增并接入统一页面命令栏、空状态、列表行和检查器组件；资料库在 1520px 以下自动切换为双栏 list-detail，900px 以下将书架范围收为按需面板。
- 知识沉淀改为紧凑书目范围栏、单行功能切换和内容工作区；文献工作台移除四个顶层业务标签，只保留“选择范围—编辑提纲—来源审计—生成与验收”主步骤条。
- “获取或导入全文”“来源与权利”迁入资料范围面板的次级抽屉，“输出记录”改为命令栏入口；原输出门禁并入汇报目标面板，不再形成第三套编号流程。
- 设计基线更新为“克制型古籍研究台”：48px 顶栏、暖赭选中态、8/12/16px 圆角层级；普通面板无阴影，按钮仅保留 120–180ms 颜色/边框反馈，不再普遍位移或缩放。
- 阅读器在 1180px 以下自动让出应用侧栏，1100px 以下目录默认为按需覆盖层；PDF 正文保持主区域，连续/单页/双页、高亮、划线和可选批注逻辑不变。
- 问答、设置和 AI 绘图迁入同一设计令牌、空状态与响应式规则，不新增路由或大卡片。
- 文献档案新增发表状态、可见范围、是否允许演示和元数据可信度；异常未来年份在资料列表显示为“年份待核对”，新保存年份限制为当前年份后一年度以内。
- 全局任务中心新增安全取消、取消中/已取消状态，以及 OCR/解析超过两分钟无进展提示；已完成页面缓存保留，后台任务仍保持 FIFO 串行以控制内存峰值。
- 资料列表查询只聚合当前页；资料库、知识范围、问答、刷题、统计、绘图和文献工作台统一使用服务端题名/作者/DOI 搜索，不再受前 100 本硬上限影响。
- 问答历史与 PPTX 输出记录改为“分页摘要列表 + 点击按需详情”；本机实测 PPTX 列表响应体由约 179KB 降至 5.6KB。
- DOCX/PPTX 结构文本标注新增章节、结构序号和字符区间锚点，支持无批注高亮、划线及从标注精确回跳，不再只保存 `page=0`。
- 设置页新增存储占用与手动缓存治理，只允许清理 OCR、Office 页面和 PPTX 预览等可重建缓存；原始文献、数据库、备份和成品始终受保护。
- PowerPoint 能力探测改为真实 COM 自动化探测；不可用时保留可编辑 PPTX 并降级为结构验收。复杂多面板、矢量密集或公式页采用 240 DPI 原页保真并记录质量边界。
- OCR 增加页级进度、取消检查点和单页无进展超时，默认 180 秒；超时或取消后保留已完成页缓存。
- 新增 1,000 本 / 100,000 chunks 合成容量基线、SQLite 三级容量护栏和 PostgreSQL 双写迁移路线。
- 新增版本化固定检索集，持续报告 Recall@1/3/5、MRR、引用正确率和无答案拒答率；新增桌面与移动端关键路由只读巡检。

### 验证
- 使用截图对应的真实 PDF 回归：JBIG2 白屏页非白像素采样由 27 恢复至 2900，GBK/CID 字体页由 234 恢复至 2309；`JBig2 failed` 与 `unexpected EOF in bcmap` 均归零。5186 字 AI 报告笔记成功显示约 5096 个可见字符和 12 个 Markdown 标题。
- Windows 11 实测“完全停止 → `study-assistant://open` → 健康检查 → 打开首页”为 5.8 秒；人为占用 8000 时自动切换 8001，PID 与真实监听进程一致。生产前端未发现 Google Fonts、jsDelivr、unpkg 或 cdnjs 引用。
- 后端 124 项测试通过、1 项按 PowerPoint 环境跳过，前端 13 项测试及 Vite 生产构建通过；桌面与 390px 移动端 24 个关键路由全部通过。1,000 本 / 100,000 chunks 合成库 FTS Top-10 P95 为 9.319ms；固定检索 v1 冒烟集通过全部最低阈值。Writing DNA 的 20 篇门禁、信息守恒、跨 run Word 替换、DOCX 原稿重放与华文中宋输出均有专项回归。

## [1.2.0] - 2026-08-26

### 改进
- 重整仓库根目录：仅保留 GitHub 规范文件、主依赖和 `start.bat` / `stop.bat`；运行时、协议、中文快捷入口和可选依赖分别迁入 `scripts/runtime`、`scripts/protocol`、`scripts/launchers` 与 `requirements`。
- README 首页改用五张 v1.2.0 本机真实运行截图，重写产品定位、差异化说明、页面导览、版本状态与脚本入口，不再使用概念 SVG 作为主视觉。
- 综合研读默认改为 DeepSeek 主导的自适应两阶段分析：先形成可见的子问题、分析维度和证据需求，再按路径检索与综合；不再套用固定 Nature/期刊报告模板。
- 新增“AI 自主研读、观点与理论比较、证据与方法审查、研究缺口探索”四种意图及快速/深度推演；深度模式两次模型调用，快速模式一次调用。
- 主张审计增加事实/关联/因果/解释类型、置信度、反例与替代解释；来源必须精确匹配本次材料锚点，越界或伪造引用自动降为待核验。
- 借鉴 PaperQA2 的迭代检索、GPT Researcher 的规划—执行—汇总和科学批判性审查方法，但不安装其运行时或新增常驻服务；检索上下文保持 48K 字符硬上限。
- 修复向量检索开启时多书查询可能不受 `book_ids` 限制的问题，现逐书检索并在融合前保持用户选择范围。
- 回归验证：后端 103 项、前端 7 项测试及 Vite 生产构建通过；1600/1065/800px 实际页面无横向溢出或控制台错误。
- “笔记与证据”升级为知识沉淀首页：高亮、划线、批注、独立知识笔记和证据卡片仅在界面聚合，均保留书目/章节/页码回链，支持类型、章节、颜色、标签、时间和全文筛选，并区分 AI 与用户来源。
- 取消“创建标注即自动创建知识树节点”的隐式写入；高亮可显式提升为独立笔记，笔记可显式加入知识树，多条证据可组合为默认待核验的主张。
- 阅读器新增无需批注的一键高亮与划线；高亮改用低透明度混合层，划线使用文字底部细线，不再遮挡原文字形。
- 知识树拖拽先展示受影响子树范围，确认后才保存；AI 展开只生成建议节点，用户确认后才写入，多本文献显示来源书目徽标且不自动合并同名节点。
- 综合研读改为三栏“研究报告工作区”，支持选择书目、章节和已有笔记，填写研究问题与对比框架，并在右侧逐条展示主张、来源锚点和“支持/部分支持/待核验/不支持”状态；结果可保存为笔记、证据卡片或发送至 PPTX 工作台。
- 目录编号推断修复同编号体系被误当父子的缺陷；`（1）→（4）`、`（一）→（二）→（三）→（一）` 等跳号/倒退链进入待复核，生成不含虚构标题的缺项占位建议；论文作者、单位和元数据行增加版面负证据过滤。
- 侧栏将笔记、知识树、知识图谱和综合研读收敛为单一“知识沉淀”入口；进入后通过职责明确的四模式切换，统一书目范围只显示一次，旧深链接自动映射到对应模式。
- 知识沉淀新增统一书目范围选择器，笔记、知识树、知识图谱和综合研读共享并持久化同一范围；取消“不选即全部”的隐式混合行为。
- 新增笔记收集箱，聚合展示但不合并阅读批注与知识笔记，支持检索、来源筛选、编辑和回到原文；多书范围下新建笔记需明确主来源。
- 知识树移除内嵌完整 PDF 和默认全量展开，改为原文摘录加阅读器回跳；子节点继承父节点书目，单本建树、多本跨树引用的边界更明确。
- 知识图谱改为概念筛选、关系探索和出处核验动线；图谱只负责发现关系，点击来源回到原文验证。
- 综合研读要求先提出具体研究问题，报告区分一致、互补、冲突与证据边界，历史报告只显示与当前书目范围完全一致的输出。
- 删除知识节点时仅解除阅读批注挂接，不连带删除原文高亮；统一接口错误提示可正确展示 FastAPI 结构化校验信息。
- 文献工作台迁移为“选择证据—设定汇报目标—审阅提纲—渲染验收”任务流，研究范围、汇报约束和输出门禁并列呈现，输出记录独立管理。
- PPTX 提纲审阅改为页面导航、当前页编辑和来源审计三栏布局；生成契约要求一页一主张、结论式标题、术语一致、证据后解释意义并显式呈现局限。
- 合法全文获取新增开放路径、馆藏登录接续和文件校验三步指引；补齐空结果、伪 PDF、浏览器跳转和入口保存的可行动反馈。
- 来源与权利页增加资源、可复用与待核对计数；PPTX 演讲者备注写入页面主张、章节/页码定位及证据摘录，便于回到原文核验。
- 资料库迁移为紧凑命令区与“智能视图/书架—阅读导向列表—资料检查器”三层工作区；大屏按需显示检查器，中小屏使用详情抽屉。
- 新增阅读中、收藏、待处理、未归档智能视图；全文检索改为按需展开，导入队列与批量归档反馈集中到当前资料上下文。
- 资料检查器汇总阅读状态、解析状态、顶级章节和关键词，并提供阅读、汇报、全文检索与完整档案入口；详情只在选中文献后加载，目录预览限制为 12 个顶级节点。
- 资料库空状态、导入、检索、智能归类和深度分析提示改为明确说明结果与下一步操作，采用可行动文案。
- 启动“全局统一、分阶段重排”方案：新增界面/阅读字体、字号、间距、圆角、纸张表面和语义文字色令牌，古籍节气主题保持不变。
- 阅读器文献头改为题名与研究操作、视图与状态两级布局；长题名最多显示两行，响应式断点提前到 1280px，200% 缩放及窄窗不再与操作区重叠。
- PDF 目录升级为默认展开的“文档导航”，宽度为 250–320px，显示层级、当前章节、页码和条目数；无目录时明确提示，并可直接进入目录结构工作台。
- PDF 工具栏按视图、页码、显示和研究工具分组；中等宽度将研究动作收纳为菜单，减少换行对正文高度的侵占。
- 阅读路由压缩全局页头，资料库与文献工作台开始复用统一页面骨架和排版令牌。

## [1.1.0] - 2026-08-25

### 文档
- 重写仓库产品介绍，以“个人文献知识库”用户旅程组织核心能力、快速开始、数据边界与技术架构。
- 新增不含真实用户书目与数据的产品界面示意图，并修正源码首次安装和 Windows 协议入口说明。

### 新增
- DOCX/PPTX 使用本机 Microsoft Office 按需渲染高保真 PDF，按文件哈希缓存，Office 完成后立即退出
- 论文页面解析保存图像引用、坐标、阅读顺序和图注关联，双栏内容按栏恢复阅读顺序
- 论文网页解析器：发现 citation PDF、iframe/下载链接及站点规则，区分开放下载与 Chrome 登录交接
- 备用 OpenAI Chat Completions 兼容接口管理，支持文本/多模态能力登记与连通性检测；默认文本分析仍锁定 DeepSeek
- 全书目录编号逻辑审计：分父级检查缺号、重号、倒序、前缀、页码与层级冲突
- 同页阅读顺序自修正，可将唯一续接的后置子项回挂到正确父项
- 阅读器目录校正器：改标题/层级/页码/顺序、新增删除、返回原页及修订快照
- 多级虚拟书架：一书多归属、子书架、未归档筛选与批量归档，不移动原文件
- PPTX 可编辑提纲、章节分层取样与覆盖率报告
- 主张—来源一致性审计，包含 source ID、数字、关键词与定位检查
- SI/图表/数据集来源权利模型与个人、内部、公开、商业用途门禁
- Windows PowerPoint 真实逐页 PNG 渲染、预览与像素级视觉回归报告
- 知识图谱 / 双向链接：自动抽概念（关键词 + 定理名，过滤停用词）→ 全局力导向图谱 → 点概念反查所有出处（章节/页码）
- 批注自动回流知识树：创建批注时自动创建知识树节点并关联
- 学习计划与目标：设定考试日期 → 按掌握度（弱→强）倒推每日任务 → 打卡日历
- 文献归档档案：作者/期刊/年份/DOI/arXiv、来源路径、阅读状态、收藏、评分和进度
- chunk—章节—PDF 页码 source map 与固定 01–16 节证据型 Paper Card
- 阅读器“原版阅读 / 结构精读 / 证据卡片”三模式及资料库归档筛选
- 文献工作台：选章/选段生成中文可编辑 PPTX，含论文类型路由、来源备注、图像提取和质量审计
- 合法全文入口：开放获取 PDF、arXiv、Unpaywall 与学校图书馆/CARSI 浏览器交接，保存无秘密来源清单

### 改进
- PDF 划线升级为多页分段锚点：修复文本命中、滚动浮层偏移和高亮遮挡，扫描页按当前页生成并缓存 RapidOCR 透明文字层
- 历史 PDF 批注支持原文自动校准和人工重新定位，后端拒绝越界、零尺寸与超页码坐标
- 仓库根目录保留稳定入口和 GitHub 约定文件，旧启动器与维护工具分别归档到 `scripts/legacy`、`scripts/maintenance`
- PDF 缩放会取消旧渲染并按新倍率重绘，限制单页像素预算、只保留可见页并提供页级失败重试
- 目录编辑器改为“虚拟目录树—原页预览—节点检查器”三栏工作台，增加撤销、问题队列和整组层级操作
- Word/PPT 阅读器区分原版页面与结构文本；结构抽取补充表格、图片、图表和分组形状
- 修正设置页云端数据说明；视觉接口可执行真实连通性检查，不再仅凭已填写 Key 判断可用
- 修复应用回退至 8001–8010 端口后新建书架等写操作被 Origin 防护误拒绝；健康检查增加 API 能力版本，所有启动入口不再复用缺少书架路由的旧后端；书架区增加明确的新建入口及创建后自动进入
- “我的资料”由拥挤的多列表格重构为阅读导向文献条目，统一书架上下文、筛选反馈、批量归档、导入确认和主次操作层级
- 前端改用 History 路由并由 FastAPI 提供 SPA fallback，工作台与阅读器深链接可直接打开
- 精简 GitHub 文档目录，移除已被当前产品文档、交接文档和轻量解析管线承接的旧 PRD、路线图、阶段决策及一次性审计记录，并新增权威文档索引
- 统一所有卡片为 gray-200 极细边框与 shadow-sm 柔和阴影；按钮增加 hover/active 反馈动画、禁用态保护和 reduced-motion 无障碍降级
- 目录识别改为融合 PDF 书签、全文编号与字号版面，支持同页多标题和中文多级编号
- 深度分析新增目录扁平度检查及带页码候选的 AI 结构复核
- OCR/PDF 断行重排按语言边界恢复段落，Markdown 不再把物理换行强制渲染为断句
- 重构资料库与阅读器布局、状态反馈和移动端响应逻辑
- 阅读器新增专注模式、字号/栏宽控制及“选段生成汇报”入口

## [1.0.0] - 2026-08-16

### 新增
- 资料导入（PDF/DOCX/PPTX）、章节树、中文全文搜索
- PDF 原生阅读器（pdf.js）：批注、高亮、位置记忆、视觉解读
- AI 问答（DeepSeek RAG，flash/pro 切换，出处定位）
- 知识树（大纲 + 思维导图双视图、跨树引用、掌握度、AI 生成/展开）
- 深度分析（三级目录 → AI 补全 → 逐章精读 → Markdown）
- AI 研读（综合阅读报告 + 思维训练：出题训练 / 自由陪练）
- 刷题自测、错题本、学习统计
- 文献自动分类、Word/PPT 阅读器

### 安全
- Markdown/HTML 输出统一 DOMPurify 消毒（XSS 防护）
- CORS 白名单 + Origin 校验（防恶意网页跨源调用）
- 后台任务 FIFO 串行 + 每任务独立 Session
- `stop.bat` 改用 PID 文件安全停止

### 工程
- 依赖锁定精确版本；测试依赖拆分 `requirements-dev.txt`
- 新增 MIT 许可证、PRIVACY.md、SECURITY.md、CI
