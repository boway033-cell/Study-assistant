# 隐私说明（Privacy）

学习助手（Study assistant）是一款**本地优先**的学习软件。本文准确说明哪些数据留在本机、哪些会发送到云端。

## 1. 始终留在本机

- 上传的 PDF / Word / PPT 原始文件
- 解析出的文本、章节、切片、全文索引（SQLite `backend/data/study.db`）
- 你的笔记、批注、高亮、知识树、题目、学习记录、统计
- API Key（保存在 `.env` 或数据库设置，仅本机）

## 2. 在本机处理（不联网）

- 文本解析、版面分析、文本清洗、关键信息提取
- 中文分词（jieba）+ 全文检索（FTS5）
- 可选向量检索（ChromaDB，默认关闭）
- 知识图谱（概念提取、共现关系、出处反查）——纯本地，不发云端
- 学习计划（日期倒推、掌握度分配、打卡记录）——纯本地，不发云端

## 3. 会发送到云端（仅在你启用 AI 功能且已配置对应 API Key 时）

| 功能 | 发送内容 | 接收方 |
|---|---|---|
| AI 问答 | 你的提问 + 检索到的教材片段 | DeepSeek |
| 深度分析 | 逐章正文（每章最多约 1.2 万字） | DeepSeek |
| 自动出题 | 教材片段 | DeepSeek |
| 综合研读 / 思维训练 | 多份资料内容 | DeepSeek |
| 知识树 AI 生成 / 展开 | 章节目录 + 关键词 | DeepSeek |
| 视觉分析（解读本页） | 页面图像 | Qwen-VL（阿里百炼） |
| 中文 PPTX 提纲（可选） | 用户明确选择的章节/选段及其来源编号 | DeepSeek |
| Writing DNA 蒸馏（可选） | 至少 20 篇语料的本地量化统计 + 每篇受限的首/中/尾代表片段 | DeepSeek |
| Writing DNA 独立仿写（可选） | DNA 产物 + 5 篇主题相近文章的受限代表片段 + 用户写作要求 | DeepSeek |
| 文本 / Word 去 AI 味（可选） | 用户提交的文本或 Word 段落/表格文字；按字符上限分批发送 | DeepSeek |

**未配置对应 API Key 时，上述云端功能不会触发，应用完全离线可用。**

写作实验室不会上传 DOCX 文件本身或 Word 样式、图片与附件，但会发送其中待分析的文字。原始 Word、Writing DNA 版本、改写审计和输出 Word 均保存在本机 `backend/data/writing/`。用户应确保对语料拥有处理权限，付费、私密或未发表语料不应在无授权情况下提交给第三方模型。

## 4. 合法全文获取与浏览器边界

- 开放获取导入会访问用户提供的 HTTPS PDF、arXiv 或 Unpaywall，并在本机校验 PDF 签名、大小和 SHA-256。
- 学校图书馆/CARSI 采用浏览器交接：只构造检索入口，由当前浏览器完成登录；应用不读取 Cookie、密码、localStorage 或会话文件。
- 应用不会绕过付费墙、DRM、验证码或双因素认证，也不会把登录页保存为 PDF。
- 获取清单记录来源、路由、大小、哈希、补充材料选择和失败原因，不包含访问凭据。

## 5. 数据备份与卸载

- **备份**：复制整个 `backend/data/` 目录（数据库 + 上传文件 + 可选向量库）；或运行 `backup.bat` 一键打包。
- **卸载**：停止服务后直接删除项目目录即可。应用所有数据只写在项目目录内，不修改系统其它文件。如需保留数据，先备份 `backend/data/`。
