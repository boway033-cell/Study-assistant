# 脚本目录

- 根目录 `start.bat`、`stop.bat` 及中文入口是面向用户的稳定启动入口。
- `maintenance/` 保存备份等日常维护工具。
- `legacy/` 保存旧快捷方式兼容脚本，不作为新用户首选入口。
- 其余脚本是版面分析、Office/PowerPoint 渲染和工作区维护的内部工具。

备份命令：`scripts\maintenance\backup.bat`。
